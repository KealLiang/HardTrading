请在原有「永久投资组合」Python回测工具基础上，新增「动态现金比例优化模块」，核心要求如下：

#### 一、核心规则（新增）
1. 新增开关参数：`dynamic_cash_switch`（布尔值，True=开启动态现金，False=关闭，关闭时等价于原等权策略）；
2. 动态调整逻辑（仅开启时生效）：
   - 仅调整「股票」和「现金」的仓位占比，「债券」「黄金」始终保持25%不变；
   - 估值判断标的：暂不知如何获取PE数据，可以用test验证看看，或者有什么近似的估算方式；
   - 估值分位规则：
     ① 沪深300 PE处于「历史0-30%分位（低估）」：股票仓位=35%，现金仓位=15%，债25%，金25%；
     ② 沪深300 PE处于「历史30-70%分位（中性）」：恢复原等权（各25%）；
     ③ 沪深300 PE处于「历史70-100%分位（高估）」：股票仓位=15%，现金仓位=35%，债25%，金25%；
   - 估值分位计算：支持「全历史分位」和「滚动10年分位」两种模式，可通过参数切换；
   - 调整频率：每月第一个交易日根据最新估值分位调整仓位（若分位区间未变化，则不调整）；
   - 再平衡优先级：先按估值调整股/现仓位，再执行原周期再平衡（月度/季度/年度）。

#### 二、数据源补充要求
1. 本地数据需新增字段：沪深300的PE（市盈率）、PB（市净率）、估值分位（全历史/滚动10年）；
2. 若本地无直接估值数据，支持手动输入「估值分位区间」（低估/中性/高估）作为替代参数。

#### 三、功能适配（新增/优化）
##### 1. 回测功能优化
- 输入参数新增：`dynamic_cash_switch`（开关）、`valuation_calc_mode`（估值分位模式：全历史/滚动10年）；
- 回测结果新增对比维度：
  ① 开启/关闭动态现金的收益对比（累计收益、年化收益、最大回撤）；
  ② 每次估值调整的记录（调整日期、调整前仓位、调整后仓位、估值分位依据）；
  ③ 按估值区间统计收益（低估/中性/高估阶段的组合表现）。

##### 2. 跟踪模式优化
- 输出新增：当前沪深300估值分位、建议仓位（是否需要调整股/现比例）、调整金额明细；
- 若开关关闭，跟踪模式输出与原策略一致。

#### 四、输出要求
1. Markdown结果新增「动态现金优化分析」板块，清晰展示：
   - 开关开启/关闭的核心指标对比表；
   - 估值调整操作记录明细；
   - 不同估值区间的收益贡献；
2. 保留原策略所有输出格式，仅新增优化相关内容。

#### 五、代码要求
1. 新增功能封装为独立函数，不修改原策略核心逻辑；
2. 关键逻辑注释（如估值分位计算、仓位调整优先级）；
3. 兼容原策略所有参数，开关关闭时完全复用原代码逻辑。
4. 输出结果需明确区分「原策略收益」和「优化后收益」，便于对比效果。